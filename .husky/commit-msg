#!/bin/sh

# BUTTERFLY commit-msg hook
# Validates commit message format using commitlint

# Exit on any error
set -e

# Run commitlint to validate commit message format
# Expects conventional commit format: <type>(<scope>): <subject>
npx --no -- commitlint --edit "$1"

